第三周筆記
編譯器
1.BNF/EBNF/生成語法

2.運算式編譯器

3.lexer

4.if ~ while

fopen
r -- 讀取。檔案必須存在

w -- 新建檔案寫入。同名檔案若存在則清空，並被視為新檔案

a -- 附加。追加的數據在文件末尾。該文件如果不存在會被創建

r+ -- 讀、更新。打開檔案並寫入，檔案必須存在

w+ -- 寫、更新。創建檔案並打開以進行更新或清空同名檔案內容

a+ -- 資料附加到舊檔案後面(游標指在EOF)，可讀取資料

b -- 二進位模式

rw+ -- 可讀取可寫入，檔案若存在就直接寫入，不存在就創建新的檔案
